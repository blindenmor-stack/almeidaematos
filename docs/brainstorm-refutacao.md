# Refutação adversarial — brainstorm-paginas-escritorio-contato.md

| # | Achado | Severidade | Evidência | Correção sugerida |
|---|---|---|---|---|
| 1 | O working tree JÁ está num estado quebrado: plumbing (E5) rodou sem as páginas (E3/E4) existirem nem `vite.config.js` atualizado. Buildando/pushando agora, as 2 URLs viram 404 — o oposto do objetivo do doc. Contradiz a frase "inverter plumbing e HTML não muda nada". | alta | `git status --short`: `M scripts/generate-posts.js`, `M api/blog/sitemap.js`, `M public/blog/posts/posts-data.json` e `blog/posts/posts-data.json` (slugs já removidos), `D public/blog/posts/advocacia-sao-paulo.json` e `contato-advogado-sao-paulo.json` (deletados); `advocacia-sao-paulo/index.html` e `contato-advogado-sao-paulo/index.html` não existem; `vite.config.js` sem entrada nova (grep vazio) | Não commitar/pushar plumbing isolado de E3/E4. Reverter os 6 arquivos já tocados ou terminar E3/E4+vite.config.js no mesmo lote antes de qualquer `npm run build` |
| 2 | DB1 (telefone fixo) e DB5 (bios dos sócios) estão listadas como "decisões que só o Bernardo toma", mas a seção 0 do próprio doc diz que telefone/horário é do "adm" e "o que dizer sobre eles" é dos sócios | alta | doc linha 13 ("sócios: o que dizer sobre eles; adm: telefone/horário") vs linhas ~193 e ~197 (DB1, DB5 na tabela "Bernardo decide") | Mover DB1→pendência do adm (já é P02) e DB5→pendência dos sócios (já é P04); Bernardo só valida o default |
| 3 | `public/blog/posts/posts-data.json` e `blog/posts/posts-data.json` NÃO são "2 cópias" (C05/E5) — são datasets diferentes: 491 vs 29 entradas, overlap de 28. O de `blog/posts/` (raiz) é só o 2º candidato de um fallback silencioso, nunca copiado pro `dist/` pelo build | média | `api/blog/sitemap.js:27-39` (`readLegacyPosts` com array de candidates); contagem: 491 vs 29 entradas, interseção 28 | Reescrever C05/E5: "remover o slug dos dois arquivos (não são cópias, um é fallback stale) sem tentar igualá-los" |
| 4 | Nome do evento errado: doc (C13, E7, E8) usa "whatsapp_click"; o código usa `click_whatsapp` em todo lugar | média | `main.js:226`, `blog-post.html:249`, `blog/index.html:266`, `api/_lib/template.js:41` — todos `event: 'click_whatsapp'` | Corrigir a grafia em C13/E7/E8 antes de consultar o GA4 no D+30 |
| 5 | C09 afirma "zero links internos hoje". O widget de relacionados filtra por `categorySlug`, e os 2 posts tinham `categorySlug: "uncategorized"` — categoria com 57 posts no total, logo é plausível que já aparecessem como "relacionado" em algum desses posts | média | `blog.js:353-370` (`renderRelatedPosts`, filtro `p.categorySlug === currentPost.categorySlug`); diff mostra `"categorySlug": "uncategorized"` nos 2 posts removidos; contagem "Uncategorized": 57 | Trocar "zero links internos" por "sem link de navegação/menu; pode ter aparecido em relacionados" |
| 6 | Plano (E5) não toca `vercel.json`. O padrão já estabelecido no repo para página estática fora do blog é excluir o prefixo do regex catch-all — `beneficios` e `politica-de-privacidade` já estão na negative lookahead | média | `vercel.json:90` (`(?!blog...|beneficios(?:/|$)|politica-de-privacidade(?:/|$)|...)`); premissa "arquivo físico vence rewrite" documentada em `api/blog/post.js:4-5` e comprovada pelos 491 posts legados (nenhum excluído individualmente, todos funcionam) | Adicionar `advocacia-sao-paulo(?:/|$)|contato-advogado-sao-paulo(?:/|$)` à mesma exclusão, por consistência e pra não depender só da precedência implícita |
| 7 | T7 estima "4 a 6h no total", mas a soma serial das faixas de E1 a E7 é 6,5h a 10,5h. Só fecha se E2 rodar em paralelo a E1 e E3 em paralelo a E4 — o doc não deixa isso explícito | baixa | Soma manual: E1(1-2h)+E2(0,5-1h)+E3(2-3h)+E4(1-2h)+E5(1h)+E6(0,5-1h)+E7(0,5h) = 6,5-10,5h serial vs "4 a 6h" declarado | Explicitar no doc se a Faixa é tempo-parede (com paralelismo) ou soma de esforço, e qual etapa roda em paralelo com qual |
| 8 | Tabela GA4 (C12) não tem export/query salvos no repo (nada em `docs/`) nem a property `404445930` é verificável nesta auditoria | baixa | `find docs -type f` não retorna nenhum arquivo de dados GA4 | Salvar o export/query usado (ex. `docs/qa/ga4-baseline.json`) antes de citar como baseline em E7/E8 |

## O que está correto e verificado

- C01: SSG gera `dist/{slug}/index.html` (`scripts/generate-posts.js:58-61`); `package.json` confirma que `generate-posts.js` roda DEPOIS de `vite build`.
- C02: meta description = `post.excerpt` cru quando não há `metaDescription` (`api/_lib/post-renderer.js:106`) — bate com o texto em caixa alta visto no diff dos posts removidos.
- C03: redirect `/wp-content/:path*` → `/` 301 confirmado (`vercel.json:13-17`).
- Não há `cleanUrls`/`trailingSlash` no `vercel.json` — comportamento padrão da Vercel, consistente com o doc.
- C05 (premissa central): "arquivo físico vence rewrite" está documentada no próprio código (`api/blog/post.js:4-5`) e comprovada em produção pelos 491 posts legados, nenhum excluído individualmente no regex.
- C06/C07: `legacy-slugs.js` tem 491 entradas incluindo os 2 slugs-alvo, e NÃO é regenerado automaticamente no build (nenhum script reescreve o arquivo) — seguro deixá-lo como está.
- C08: schema `@graph` (LegalService + BreadcrumbList + FAQPage), `hero--page`, `section--paper`, `cta-final` confirmados em `beneficios/auxilio-acidente/index.html`.
- C09: seção `#escritorio`, endereço novo (Alameda Araguaia, 1142) e "70+ profissionais" confirmados em `index.html`.
- C13: `.whats-fab` e `wa.me/5511930044411` com texto pré-preenchido confirmados; o tracking é site-wide via `main.js` (delegação por seletor CSS, não por URL) — páginas novas herdam automaticamente ao incluir `/main.js`.
- C14: exatamente 12 arquivos HTML com `<footer>` duplicado, confirmado via grep.
- Clarity e Manychat são injetados via plugin do Vite (`transformIndexHtml`) — páginas novas herdam automaticamente só por entrarem em `rollupOptions.input`, sem editar HTML manualmente.
